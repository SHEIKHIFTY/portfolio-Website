/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['placehold.co'], // Add 'placehold.co' to the list of allowed image domains
  },
};

module.exports = nextConfig;
